name: micro_ml

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  training-endpoint:
    name: training
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: "Set up Python"
        uses: actions/setup-python@v5
        with:
          python-version-file: ".python-version"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install the project
        run: uv sync

      - name: Run training script
        # We use 'uv run' to ensure the script uses the environment created by 'uv sync'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: uv run train

  docker:
    name: build and push docker image
    runs-on: ubuntu-latest
    needs: training-endpoint
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build & push training image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.training
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/micro-ml-training:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/micro-ml-training:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build & push inference image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.inference
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/micro-ml-inference:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/micro-ml-inference:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max